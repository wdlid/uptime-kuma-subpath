services:
  uptime-kuma:
    image: wdlid/uptime-kuma-subpath:latest
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    ports:
      - "3001:3001"
    environment:
      # Set subpath if running behind a reverse proxy (e.g. /uptime/)
      # UPTIME_KUMA_BASE_PATH: /uptime
